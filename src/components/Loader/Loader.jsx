import { LoaderWrapper } from './Loader.styled';
import { Icon } from '../ui/Icon';

const Loader = () => {
  return (
    <LoaderWrapper>
      <Icon />
    </LoaderWrapper>
  );
};

export default Loader;
